# Release Notes

## 2026-01-09

- Added documented build via PyInstaller `.spec` (preferred) using "Debutade Apps Starter.spec" and noted Python/PyInstaller versions used.
- Updated README Files section to list all available `.spec` files (primary, alternate, variant) and marked the legacy typo-named spec as not used.
- General documentation cleanup for build and usage.

## 2026-01-08

- Fixed config loading in EXE (bundled `start-debutade.config` and improved path resolution).
- Prevented duplicate launcher when starting webservices from the EXE.
- Added Build section with PyInstaller command and asset bundling.
- Corrected executable name to **Debutade Start Apps.exe** and documented cleanup of typo file.
